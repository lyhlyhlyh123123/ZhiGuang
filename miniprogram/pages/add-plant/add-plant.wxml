<view class="page-container">
  
  <view class="nav-header">
    <view class="back-btn" bindtap="goBack"><text class="icon icon-back"></text></view>
    <view class="placeholder-box"></view>
  </view>

  <scroll-view scroll-y class="scroll-content">
    
    <!-- 图片上传/拖动预览框 -->
    <view class="photo-upload-box {{tempImagePath ? 'has-img' : ''}}" style="{{boxH ? 'height:' + boxH + 'px;' : ''}}">
      <view wx:if="{{!tempImagePath}}" class="upload-placeholder" bindtap="chooseImage">
        <text class="icon icon-image"></text>
        <text class="upload-title">点击上传植物照片</text>
        <text class="upload-subtitle">选一张它最好看的瞬间</text>
      </view>
      <block wx:else>
        <movable-area style="width:100%;height:{{boxH}}px;overflow:hidden;">
          <movable-view
            direction="all"
            x="{{imgX}}" y="{{imgY}}"
            bindchange="onImgMove"
            scale
            bindscale="onImgScale"
            style="width:{{imgW}}px;height:{{imgH}}px;"
          >
            <image src="{{tempImagePath}}" mode="scaleToFill" style="width:{{imgW}}px;height:{{imgH}}px;display:block;"></image>
          </movable-view>
        </movable-area>
        <view class="crop-hint-bar">
          <text class="crop-hint-text">拖动/双指缩放调整显示区域</text>
          <view class="crop-reselect" catchtap="chooseImage">重新选择</view>
        </view>
      </block>
    </view>

    <!-- 隐藏 canvas 用于截图 -->
    <canvas canvas-id="cropCanvas" class="hidden-canvas" style="width:{{boxW}}px;height:{{boxH}}px;position:fixed;top:-9999px;left:-9999px;"></canvas>

    <view class="section-title">基础信息</view>
    <view class="info-group">
      <view class="input-wrapper">
        <text class="input-label">植物昵称</text>
        <input class="styled-input" placeholder="给你的伙伴起个名字吧..." value="{{nickname}}" bindinput="onNicknameInput" />
      </view>
      
      <view class="input-wrapper">
        <text class="input-label">植物品种</text>
        <input class="styled-input" placeholder="例如：龟背竹、多肉..." value="{{species}}" bindinput="onSpeciesInput" />
      </view>
      
      <view class="input-wrapper">
        <text class="input-label">摆放位置</text>
        <input class="styled-input" placeholder="例如：阳台、客厅、书桌..." value="{{location}}" bindinput="onLocationInput" />
        <view class="quick-tags">
          <view class="tag-chip" bindtap="quickSetLocation" data-val="阳台">阳台</view>
          <view class="tag-chip" bindtap="quickSetLocation" data-val="客厅">客厅</view>
          <view class="tag-chip" bindtap="quickSetLocation" data-val="书房">书房</view>
          <view class="tag-chip" bindtap="quickSetLocation" data-val="卧室">卧室</view>
        </view>
      </view>
      
      <view class="input-wrapper">
        <text class="input-label">到家日期 <text style="color:#9CA3AF;font-weight:400;">（选填）</text></text>
        <picker mode="date" value="{{adoptDate}}" bindchange="onDateChange">
          <view class="picker-text {{adoptDate ? '' : 'placeholder-color'}}">
            {{adoptDate || '选填，记录它来到你身边的日期'}}
          </view>
        </picker>
      </view>
    </view>

    <view class="section-title mt-8">养护设定</view>
    <view class="settings-card">
      
      <view class="setting-item no-border">
        <view class="setting-info">
          <view class="setting-icon-wrap bg-blue">
            <text class="icon icon-water" style="font-size: 40rpx; color: #3B82F6;"></text>
          </view>
          <view class="setting-text">
            <text class="setting-name">浇水周期</text>
            <text class="setting-desc">每隔 {{waterInterval}} 天浇一次水</text>
          </view>
        </view>
        <view class="stepper">
          <view class="step-btn" bindtap="minusInterval">-</view>
          <text class="step-val">{{waterInterval}}</text>
          <view class="step-btn text-green" bindtap="addInterval">
            <text class="icon icon-plus" style="font-size: 24rpx;"></text>
          </view>
        </view>
      </view>
      
    </view>

  </scroll-view>

  <view class="bottom-bar">
    <button class="submit-btn" bindtap="submitPlant">加入我的花园</button>
  </view>

</view>