<!--pages/edit-plant/edit-plant.wxml-->
<view class="page-container">

  <view class="nav-header">
    <view class="back-btn" bindtap="goBack"><text class="icon icon-arrow-left-bold"></text></view>
    <text class="nav-title">编辑植物资料</text>
    <view class="placeholder-box"></view>
  </view>

  <scroll-view scroll-y class="scroll-content">

    <!-- 多图片管理区域 -->
    <view class="section-title">植物照片 ({{imageList.length}}/{{maxImageCount}})</view>
    <view class="image-upload-container">
      <scroll-view scroll-x class="image-scroll-view">
        <view class="image-list">
          <!-- 已有的图片 -->
          <view class="image-item {{selectedIndex === index ? 'selected' : ''}}" wx:for="{{imageList}}" wx:key="index" bindtap="onImageTap" data-index="{{index}}">
            <image src="{{item.url}}" mode="aspectFill" class="image-preview" />
            <!-- 封面标记 -->
            <view wx:if="{{index === 0}}" class="cover-badge">封面</view>
            <!-- 删除按钮 -->
            <view class="delete-btn" catchtap="deleteImage" data-index="{{index}}">
              <text class="icon icon-delete"></text>
            </view>
          </view>
          
          <!-- 添加按钮 -->
          <view wx:if="{{imageList.length < maxImageCount}}" class="add-image-btn" bindtap="chooseImages">
            <text class="icon icon-plus"></text>
            <text class="add-text">添加照片</text>
            <text class="add-hint">还能添加{{maxImageCount - imageList.length}}张</text>
          </view>
        </view>
      </scroll-view>
      <view class="upload-tip">
        <text class="icon icon-info"></text>
        <text>点击两张图片可交换位置，第一张照片为封面</text>
      </view>
    </view>

    <view class="section-title">基础信息</view>
    <view class="info-group">
      <view class="input-wrapper">
        <text class="input-label">植物昵称</text>
        <input class="styled-input" placeholder="给你的伙伴起个名字吧..." value="{{nickname}}" bindinput="onNicknameInput" />
      </view>

      <view class="input-wrapper">
        <text class="input-label">植物品种</text>
        <input class="styled-input" placeholder="例如：龟背竹、多肉..." value="{{species}}" bindinput="onSpeciesInput" />
        <view class="category-tags">
          <scroll-view scroll-x class="tags-scroll">
            <view class="tag-list">
              <view class="tag-chip {{species === item ? 'active' : ''}}" wx:for="{{speciesCategories}}" wx:key="index" bindtap="selectSpecies" data-val="{{item}}">
                {{item}}
                <text class="tag-delete" catchtap="deleteSpeciesCategory" data-val="{{item}}">×</text>
              </view>
              <view class="tag-chip add-tag" bindtap="showAddSpeciesDialog">
                <text class="icon icon-plus" style="font-size: 20rpx;"></text> 添加
              </view>
            </view>
          </scroll-view>
        </view>
      </view>

      <view class="input-wrapper">
        <text class="input-label">摆放位置</text>
        <input class="styled-input" placeholder="例如：阳台、客厅、书桌..." value="{{location}}" bindinput="onLocationInput" />
        <view class="category-tags">
          <scroll-view scroll-x class="tags-scroll">
            <view class="tag-list">
              <view class="tag-chip {{location === item ? 'active' : ''}}" wx:for="{{locationCategories}}" wx:key="index" bindtap="selectLocation" data-val="{{item}}">
                {{item}}
                <text class="tag-delete" catchtap="deleteLocationCategory" data-val="{{item}}">×</text>
              </view>
              <view class="tag-chip add-tag" bindtap="showAddLocationDialog">
                <text class="icon icon-plus" style="font-size: 20rpx;"></text> 添加
              </view>
            </view>
          </scroll-view>
        </view>
      </view>

      <view class="input-wrapper">
        <text class="input-label">来源 <text style="color:#9CA3AF;font-weight:400;">（选填）</text></text>
        <input class="styled-input" placeholder="例如：花市、朋友赠送、网购..." value="{{source}}" bindinput="onSourceInput" />
      </view>

      <view class="input-wrapper">
        <text class="input-label">到家日期 <text style="color:#9CA3AF;font-weight:400;">（选填）</text></text>
        <picker mode="date" value="{{adoptDate}}" bindchange="onDateChange">
          <view class="picker-text {{adoptDate ? '' : 'placeholder-color'}}">
            {{adoptDate || '请选择它来到你身边的日期'}}
          </view>
        </picker>
      </view>

      <view class="input-wrapper">
        <text class="input-label">备注 <text style="color:#9CA3AF;font-weight:400;">（选填）</text></text>
        <input class="styled-input" placeholder="记录一些特殊的备注信息..." value="{{remark}}" bindinput="onRemarkInput" />
      </view>
    </view>

    <view class="section-title mt-8">养护计划</view>
    <view class="settings-card">

      <view class="setting-item care-plan-header">
        <view class="setting-info">
          <view class="setting-icon-wrap bg-green">
            <text class="icon icon-penzai" style="font-size: 40rpx; color: #16A34A;"></text>
          </view>
          <view class="setting-text">
            <text class="setting-name">启用养护计划</text>
            <text class="setting-desc">提醒浇水、施肥等任务</text>
          </view>
        </view>
        <switch checked="{{carePlanEnabled}}" bindchange="toggleCarePlan" color="#16A34A" />
      </view>

      <block wx:if="{{carePlanEnabled}}">
        <view class="care-task-item" wx:for="{{careTasks}}" wx:key="taskId" data-index="{{index}}">
          <view class="care-task-left">
            <switch checked="{{item.enabled}}" bindchange="toggleTaskEnabled" data-index="{{index}}" color="#16A34A" style="transform:scale(0.8);margin-right:4rpx;" />
            <text class="iconfont {{item.icon}} task-icon"></text>
            <text class="task-name">{{item.name}}</text>
          </view>
          <view class="care-task-right">
            <input class="task-interval-input" type="number" value="{{item.interval}}" bindinput="onTaskIntervalInput" data-index="{{index}}" />
            <text class="task-interval-unit">天</text>
          </view>
        </view>
      </block>

    </view>

    <view class="section-title mt-8">陪伴状态</view>
    <view class="settings-card">
      <view class="setting-item no-border">
        <view class="setting-info">
          <view class="setting-icon-wrap {{isDead ? 'bg-dead' : 'bg-green'}}">
            <text class="iconfont {{isDead ? 'icon-siwangbinzang' : 'icon-penzai'}}" style="font-size: 40rpx; color: {{isDead ? '#9CA3AF' : '#16A34A'}};"></text>
          </view>
          <view class="setting-text">
            <text class="setting-name">{{isDead ? '已结束陪伴' : '陪伴中'}}</text>
            <text class="setting-desc">{{isDead ? '点击切换为开始陪伴' : '若植物死亡，点击切换为结束陪伴'}}</text>
          </view>
        </view>
        <switch checked="{{!isDead}}" bindchange="toggleDead" color="#16A34A" />
      </view>
    </view>

  </scroll-view>

  <view class="bottom-bar">
    <button class="submit-btn" bindtap="submitPlant">保存修改</button>
  </view>

</view>